# Core dependencies for paperRAG

# PDF Processing
pymupdf>=1.23.0  # PDF parsing and text extraction

# Embeddings and Vector Search
sentence-transformers>=2.2.0  # Text embeddings
chromadb>=0.4.0  # Vector database for semantic search

# LLM Inference
# llama-cpp-python>=0.2.0  # Local LLM inference with llama.cpp (requires C++ build tools)
# Alternative: use transformers with CPU inference or install pre-built wheel later

# Database
# sqlite3 is included in Python standard library

# Utilities
numpy>=1.24.0  # Numerical operations
tqdm>=4.65.0  # Progress bars
matplotlib>=3.7.0  # Plotting and visualization
scikit-learn>=1.3.0  # Machine learning utilities (t-SNE, UMAP)

# Optional: UI (will add Gradio later in Phase 4)
# gradio>=4.0.0

# Development dependencies
pytest>=7.4.0  # Testing framework
black>=23.0.0  # Code formatting
